/*function addUp(x) {
    var sum = 0;
    if (isNumeric(x)) {
        for ()
    }
}*/
function isNumeric(x) {
    (!isNaN)parseFloat(x))
}

function calcAge (age) {
    if (isNumeric(age)) {
        return (age * 365);
    } else {
        return NaN
    }
}

function sumPolygon(n) {
    if (isNumeric(n)) && n>2) {
        return (n-2) * 100;
    }
    return "Value is not the bunber of sides of a polygon"
}

function sumAll (array) {
    var =
}